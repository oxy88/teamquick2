type LolProfile {
  id: ID! @unique
  name: String!
  tier: LolTier!
  role: LolRole!
}

enum LolTier {
    BRONZE
    SILVER
    GOLD
    PLATINUM
    DIAMOND
}

enum LolRole {
    FILL
    TOP
    JUNGLE
    MID
    ADC
    SUPPORT
}